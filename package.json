{
  "name": "badminton-match-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Badminton Match Manager - Real-time tournament management",
  "scripts": {
    "dev": "concurrently \"pnpm --filter server dev\" \"pnpm --filter client dev\"",
    "build": "pnpm --filter shared build && pnpm --filter client build && pnpm --filter server build",
    "start": "node server/dist/index.js",
    "test": "pnpm --filter server test && pnpm --filter client test",
    "test:coverage": "pnpm --filter server test:coverage && pnpm --filter client test:coverage"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "esbuild"]
  }
}
